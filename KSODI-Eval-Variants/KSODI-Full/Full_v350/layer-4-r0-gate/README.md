# Layer 4 - R0 / R_0 Gate

Status: public placeholder, pending private v350 review

`R0` is the relational gate for dyadic or n-adic observation.

It asks whether distinguishable trajectories are stable enough for relational comparison. It is not full resonance and must not be collapsed into `IK`.

